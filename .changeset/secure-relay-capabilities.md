---
"@glyph-oss/connect": major
---

Add secure relay v2 sessions with separate read and callback capabilities, support verification of wallet-signed callback envelopes, and remove the legacy relay v1 `subscribeViaRelay()` and `relayCallbackUrl()` APIs.
